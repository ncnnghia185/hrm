<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reset Password OTP</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
            text-align: center;
        }
        .container {
            max-width: 500px;
            background: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border: 1px solid #ddd;
        }
        .header {
            text-align: center;
            padding: 10px 0;
        }
        .header img {
            max-width: 150px;
        }
        .header h2 {
            font-size: 22px;
            margin: 10px 0;
        }
        .body-text {
            font-size: 16px;
            color: #333;
            margin-bottom: 20px;
        }
        .otp {
            font-size: 24px;
            font-weight: bold;
            color: #d32f2f;
            border: 1px solid #121212;
            display: inline-block;
            padding: 10px;
            margin: 10px 0;
            letter-spacing: 5px;
        }
        .info-text {
            font-size: 14px;
            margin-top: 10px;
            color: #555;
        }
        .footer {
            font-size: 15px;
            color:#e74c3c;
            margin-top: 20px;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="{{base64logo}}" alt="Company Logo">
            <h2>Reset Your Account Password</h2>
        </div>
        <p class="body-text">Use this code to reset password. This code will expire in 20 minutes.</p>
        <p class="otp">{{token}}</p>
        <p class="info-text">This code will securely reset your account <br> <strong>{{email}}</strong></p>
        <p class="footer">Please not reply this email !.</p>
    </div>
</body>
</html>
