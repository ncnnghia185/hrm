"use client";
import React from "react";
import { Formik, Form, Field, ErrorMessage } from "formik";
type Props = {};

const PermissionInfoForm = (props: Props) => {
  return <div>PermissionInfoForm</div>;
};

export default PermissionInfoForm;
